# 功能讨论与拆分目录

- `features/`：用于存放每个功能的讨论记录、需求拆分与实现指引。
  - 每个功能建议一个独立子目录（例如 `features/semantic-search/`）。
  - 子目录内可包含 `overview.md`（需求背景）、`tasks.md`（拆分步骤）、`notes.md`（会议/讨论记录）等文件。

后续如需新增其他类型的设计文档，可在 `docs/` 目录继续扩展。
